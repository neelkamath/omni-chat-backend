version: '3.8'
services:
  chat:
    build:
      dockerfile: docker/Dockerfile
      context: .
    command: java -server -jar omni-chat.jar
    environment:
      ALLOWED_DOMAINS: ${ALLOWED_DOMAINS}
